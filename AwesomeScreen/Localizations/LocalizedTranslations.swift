import Foundation

typealias L10n = LocalizedTranslations

/// `LocalizedTranslations` is an empty struct intended to delimit the namespace.
struct LocalizedTranslations {
    private init() {
        // Intentionally unimplemented. This struct is needed to delimit the namespace only.
    }
}
